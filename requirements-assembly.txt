# Stage 5 - Song assembly & rendering (CPU-friendly, runs anywhere).
# Install with:  pip install -r requirements-assembly.txt
# System dependency: ffmpeg (preinstalled on Kaggle; `apt-get install ffmpeg`
# locally) for MP3 export via pydub.

pydub==0.25.1
pyloudnorm==0.1.1
soundfile==0.12.1
librosa==0.10.2.post1
scipy>=1.10,<1.14
numpy<2.0
PyYAML==6.0.2
tqdm==4.66.5
